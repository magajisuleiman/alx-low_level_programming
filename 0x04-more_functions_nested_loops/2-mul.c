#include "main.h"

/**
 * mul - function that checks for a digit (0 through 9).
 * @a: first operand
 * @b: second operand
 *
 * Return: multipplication of two integer a and b
*/

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}

