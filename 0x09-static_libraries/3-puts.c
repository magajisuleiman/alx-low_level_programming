#include "main.h"
#include <stdio.h>

void _puts(char *str);

/**
 * main - prints a string, followed by a new line, to stdout
 *
 *
 * Return: at success 0
 */

int main(void)
{
	_puts("I love you");
	return (0);
}
